import { render } from "../../../utils/test.utils";

import { AddSheetPage } from ".";
describe("AddSheetPage", () => {
  it("should render an add button", () => {
    // const { getByTestId } = render(<AddSheetPage loadSheet={} addSheet={} />);
  });
});
